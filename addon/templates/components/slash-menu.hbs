<ul>
{{#each toolbar as |tool|}}
    <li>
        {{#if tool.icon}}
            <img src="{{iconURL}}{{tool.icon}}" />
        {{else}}
            {{tool.name}}
        {{/if}}
        {{tool.label}}
    </li>
{{/each}}
</ul>