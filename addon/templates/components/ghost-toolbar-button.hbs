{{!--for some reason binding the class isn't working--}}
<button {{action "click"}}>
    {{#if tool.icon}}
        <img src="{{iconURL}}{{tool.icon}}" />
    {{else}}
        {{tool.name}}
    {{/if}}
</button>